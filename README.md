# Phloxcoin Website

This cleaned version keeps the Phloxcoin landing page as the primary site.

## Changes
- Removed the old `Phloxcoin.html` internal page and merged its useful content into `index.html`.
- Removed the old `Ixoracoin.html` page from the main site.
- Redesigned `send-token-to-multiple-addresses.html` while preserving the existing multisend engine.
- Added a new responsive `css/modern.css` and `css/send-modern.css`.
- Existing Web3/contract logic is retained for the PLX presale controls.

## Deployment
Upload the contents of this folder to the Phloxcoin GitHub Pages repository.


v10 Market Hub: official KuCoin logo asset added locally; KuCoin PLX page is highlighted accurately as a dedicated price page, while preserving KuCoin's current status note that PLX is not officially listed.
